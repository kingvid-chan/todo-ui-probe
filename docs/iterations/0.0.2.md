# Iteration 0.0.2 — Deployment Path Fix

## Scope
server.py 子路径前缀处理修复：支持 `/projects/todo-ui-probe/` 部署路径。

## Owner Input
无新 owner input。来自 Hermes 部署需求：Nginx sub-path 代理要求服务端正确处理路径前缀。

## Changes
- `src/server.py`: `do_GET()` 增加 `/projects/todo-ui-probe/` 前缀归一化
- `/projects/todo-ui-probe/` → 映射为 `/` 返回 index.html
- `/projects/todo-ui-probe/healthz` → 映射为 `/healthz` 返回健康检查
- 路径重写后保留 `Cache-Control: no-cache` 响应头

## Tech Stack
同 0.0.1：纯前端 HTML + CSS + vanilla JS，Python 开发服务器

## Deployment
Nginx sub-path: `/projects/todo-ui-probe/`
